Invoque o agente para criar uma feat-XX a partir desta descrição: $ARGUMENTS

**Handoff:** Gere um resumo para o comando `proxima-feature`.

Após criar o `sdd/features/feat-XX.md`, use o comando `proxima-feature` para iniciar a execução — ele cria a branch `feat/*` antes de implementar.
