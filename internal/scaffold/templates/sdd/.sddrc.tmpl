{
  "project": "{{.Project}}",
  "version": "{{.SddVersion}}",
  "agents": [{{range $i, $a := .Agents}}{{if $i}}, {{end}}"{{$a}}"{{end}}],
  "telemetry": {
    "enabled": {{.Telemetry}},
    "anonymous": true,
    "endpoint": "local"
  },
  "lang": "{{.Lang}}"
}
